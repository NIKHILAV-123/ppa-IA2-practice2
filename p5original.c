#include<stdio.h>
#include<math.h>
int input()
{
  int a;
  printf("enter the numbers for which you want gcd\n");
  scanf("%d",&a);
  return a;
  
}
int gcd(int a,int b)
{
  int gcd(a,b);
  gcd(a,b) = (a*b)/lcm(a,b);
  
 {
   gcd(a,b) = (a*b)/lcm(a,b);
 }
  return gcd(a,b);
}
void output(int a,int b,int gcd)
{
  printf("the gcd value of %d and %d is %d\n",a,b,gcd);
}
int main()
{
  int a,b,gcd;
  a=input();
  b=input();
  result=gcd;
  output(a,b,gcd);
  return 0;
}

